# how-programming-is-supposed-to-be
Wizarding itself

## getting started
    python -m pip install --upgrade pip
	python -m pip install virtualenv
	python -m virtualenv env
	source env/Scripts/activate
	pip install -r requirements.txt
	./run.sh
